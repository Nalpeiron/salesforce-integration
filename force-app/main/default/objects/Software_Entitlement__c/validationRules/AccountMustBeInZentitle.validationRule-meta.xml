<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>AccountMustBeInZentitle</fullName>
    <active>true</active>
    <description>This was created to prevent using an account that is not already in Zentitle.</description>
    <errorConditionFormula>ISBLANK(Account__r.Zentitle_Customer_ID__c)</errorConditionFormula>
    <errorDisplayField>Account__c</errorDisplayField>
    <errorMessage>The account must already be in Zentitle; go to the Account and use the button to send the account to Zentitle before creating the software entitlement.</errorMessage>
</ValidationRule>
